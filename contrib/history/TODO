- script to clean old rrd
- improve src and dst list look
- add some links to facilitate navigation ie: invers src and dst, ...
- mail-alert function: 
	if something changed and that the changes are out of acceptable range
	the most important change is a loss of connectivity toward a site
	also, once a week, the list of site with problems could be posted
- filter include/exclude prefix to store
- set history to be ipv4 compatible ( change the dot separator )
- create a common lib for arch.pl and history.pl to have only one place to change format


To be confirmed:
- check values
- check if no .ssm.rrd or .asm.rrd are faulty created

